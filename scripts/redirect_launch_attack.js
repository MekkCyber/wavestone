const launchButton = document.getElementById('launch_button');

launchButton.addEventListener('click', () => {
    // Redirect to the /attackPanel location
    window.location.href = '/attackPanel';
});